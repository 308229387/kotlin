<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kotlin">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>

    <uses-permission android:name="android.permission.READ_PRIVILEGED_PHONE_STATE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.Light.NoActionBar">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.IncludeActivity" />
        <activity android:name=".activity.FragmentDemoActivity" />
        <activity
            android:name=".activity.KeyboardActivity"
            android:windowSoftInputMode="adjustNothing" />
        <activity android:name=".activity.ViewPagerActivity" />
        <activity android:name=".activity.GridActivity" />
        <activity android:name=".activity.CustomViewpagerActivity" />
        <activity android:name=".activity.ReadAssetsActivity" />
        <activity android:name=".activity.CommentTestActivity" />
        <activity android:name=".activity.BottomDialogActivity" />
        <activity android:name=".activity.ImeiActivity" />
        <activity android:name=".activity.AlgorithmListActivity" />
        <activity android:name=".activity.AlgorithmDetailActivity" />
        <activity android:name=".activity.PermissionActivity" />
        <activity android:name=".activity.GetPhoneNumActivity" />
    </application>

</manifest>